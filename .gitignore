node_modules/
nul

# Project docs (keep local only)
PROGRESS.md
CLAUDE.md
.claude/
instagram-cli-development-roadmap_1.md
instagram-cli-development-roadmap_UPDATED.md
instagram-cli-mockup.html
design_handoff_messenger_redesign/

# Build output
packages/backend/dist/

# Auth session (contains credentials)
packages/backend/session.json

# Persisted FCM device tokens
packages/backend/device-tokens.json
device-tokens.json

# TUI daemon config (contains the pairing token)
daemon.config.json
packages/tui/daemon.config.json

# FCM service-account key (private key — must never be committed)
packages/backend/fcm-service-account.json
fcm-service-account.json
*firebase-adminsdk*.json

# Logs (may contain session/token material)
*.log

# Compiled binaries
packages/tui/*.exe